name: Deploy to Staging
on:
  release:
    types:
      - created
jobs:
  Deploy-to-Staging:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: git remote
      run: git remote -v
    - name: Deploy to Staging
      run: git push --force origin main:deploy-to-staging